
{#{{form_start(f)}}
{{ form_label(f.Username) }}
{{ form_widget(f.Username) }}
{{form_end(f)}}#}
{{form(f)}}
<table border="3">
    <tr>
        <td>id</td>
        <td>username</td>
        <td>email</td>
        <td>nbrlivre</td>
        <td>edit</td>
        <td>delete</td>
        <td>action</td>
    </tr>
    {% for a in authors %}
    <tr>
        <td>{{a.id}}</td>
        <td>{{a.username}}</td>
        <td>{{a.email}}</td>
        <td>{{a.nbrlivre}}</td>
        <td><a href="{{path('editauthor',{'id':a.id})}}">edit</a></td>
        <td><a href="{{path('deleteauthor', {'id': a.id})}}">delete</a></td>
        <td><a href="{{path('showbook', {'id': a.id})}}">show</a></td>
    </tr>
    {% endfor %}
</table>