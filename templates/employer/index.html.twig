{% extends 'base.html.twig' %}

{% block title %}Employer index{% endblock %}

{% block body %}
    <h1>Employer index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Age</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for employer in employers %}
            <tr>
                <td>{{ employer.id }}</td>
                <td>{{ employer.nom }}</td>
                <td>{{ employer.age }}</td>
                <td>
                    <a href="{{ path('app_employer_show', {'id': employer.id}) }}">show</a>
                    <a href="{{ path('app_employer_edit', {'id': employer.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_employer_new') }}">Create new</a>
{% endblock %}
